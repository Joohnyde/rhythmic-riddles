# quiz-game backend (Spring Boot)

## Purpose
Backend for the live quiz game:
- durable game state in PostgreSQL
- REST endpoints for Admin/TV actions
- WebSocket push updates (server → client)

## Run (local)
See root README and `docs/developer-guide/getting-started.md`.

## Config
- `.env.example` for environment variables (copy to `.env`, gitignored)

> TODO: Add actual Maven commands once code is moved into this folder.
