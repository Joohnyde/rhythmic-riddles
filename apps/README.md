# apps/

This folder contains the application code split into backend and frontend components.

- `backend/` – Spring Boot services, REST + WebSocket APIs, asset handling, embedded DB integration, packaging hooks
- `frontend/` – Angular SPA (Admin + TV), built and copied into the backend for production builds

Documentation entry point: `docs/index.md`
